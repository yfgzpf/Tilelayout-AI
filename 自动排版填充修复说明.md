# 自动排版填充功能 - 测试指南

**修复时间**: 2026-05-06  
**问题**: 无法自动生成填充排版  
**解决方案**: 保存项目后自动调用排版计算 API

---

## 🔧 修复内容

### 问题根源
之前的代码在保存项目后**没有自动执行排版计算**，需要用户手动点击"预览排版"按钮才能触发计算。

### 修复方案
修改了 [`ProjectEdit.tsx`](file:///f:/目录已检查/TileLayout%20AI/packages/shared/src/pages/ProjectEdit.tsx) 的 `handleSave` 函数：

1. **保存成功后自动调用** `projectsApi.calculateLayout()`
2. 通过 `autoCalculate` 开关控制是否自动计算（默认开启）
3. 计算完成后自动跳转到预览页面
4. 错误处理：即使计算失败，项目也会成功保存

### 代码变更

```typescript
// 创建成功后自动执行排版计算
if (autoCalculate) {
  try {
    const result = await projectsApi.calculateLayout(newProject.id, {
      textureId: undefined,
      config: tileConfig,
      optimize: false,
    });
    setLayoutResult(result);
    message.success('排版计算完成');
  } catch (calcErr) {
    console.error('自动排版计算失败:', calcErr);
    message.warning('项目已保存，但排版计算失败');
  }
}

navigate(`/project/${newProject.id}/preview`);
```

---

## ✅ 测试步骤

### 1. 环境准备

#### 后端启动
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

#### 前端启动
```bash
cd packages/web
pnpm install
pnpm dev
```

访问：http://localhost:3000

---

### 2. 完整测试流程

#### 步骤 1: 创建新项目
1. 点击"新建项目"按钮
2. 进入项目编辑页面

#### 步骤 2: 绘制户型
1. 点击"开始绘制"按钮
2. 在画布上点击添加顶点（至少 3 个）
3. 点击"完成绘制"闭合多边形
4. 可以拖动顶点调整形状

#### 步骤 3: 配置瓷砖参数
1. 选择瓷砖规格预设（如 800×800mm）
2. 设置留缝宽度（默认 3mm）
3. 选择铺贴方向（横向/纵向/斜向）
4. **确认"自动计算"开关已开启**

#### 步骤 4: 保存项目
1. 点击"保存"按钮
2. **观察是否出现两条消息提示**:
   - ✅ "项目创建成功"
   - ✅ "排版计算完成"
3. **自动跳转到预览页面**

#### 步骤 5: 验证排版结果
1. 检查预览页面是否显示瓷砖网格
2. 检查整砖和切割砖是否正确区分
3. 查看统计信息（整砖数、切割砖数、损耗率）

---

### 3. 预期结果

#### ✅ 成功的表现
- 保存项目后**立即自动计算**排版
- 自动跳转到预览页面
- 预览页面显示完整的瓷砖填充效果
- 显示统计信息（整砖/切割砖数量）

#### ⚠️ 失败的表现
- 保存后没有自动跳转
- 预览页面显示"暂无排版结果"
- 控制台报错 "calculateLayout is not defined"

---

## 🐛 常见问题排查

### 问题 1: 保存后没有自动计算

**检查点**:
1. 浏览器控制台是否有 JavaScript 错误
2. `autoCalculate` 状态是否为 `true`
3. 网络请求是否成功（查看 Network 标签）

**解决方法**:
```bash
# 清理并重新安装前端依赖
rm -rf node_modules
pnpm install
```

### 问题 2: 排版计算 API 返回 400 错误

**错误信息**:
```json
{
  "detail": "户型轮廓至少需要 3 个顶点"
}
```

**原因**: 户型多边形顶点数不足

**解决方法**:
- 确保绘制至少 3 个顶点
- 检查 `roomPolygon` 数据格式：`[[x1,y1], [x2,y2], [x3,y3], ...]`

### 问题 3: 排版计算 API 返回 500 错误

**错误信息**:
```json
{
  "detail": "排版计算失败：..."
}
```

**检查后端日志**:
```bash
# 查看后端控制台输出
# 或查看日志文件
tail -f backend/logs/app.log
```

**可能原因**:
- 多边形不闭合
- 瓷砖尺寸为 0 或负数
- 留缝宽度为负数

---

## 📊 性能测试

### 测试场景

#### 场景 1: 小户型（10㎡）
- 瓷砖规格：300×300mm
- 预期计算时间：< 0.5 秒

#### 场景 2: 中户型（50㎡）
- 瓷砖规格：800×800mm
- 预期计算时间：< 1 秒

#### 场景 3: 大户型（200㎡）
- 瓷砖规格：600×1200mm
- 预期计算时间：< 3 秒

### 性能优化建议

如果计算时间超过预期：

1. **后端优化**:
   - 启用 Redis 缓存排版结果
   - 使用异步任务队列（Celery）

2. **前端优化**:
   - 添加计算进度提示
   - 使用 Web Worker 避免阻塞 UI

---

## 🔍 调试技巧

### 前端调试

```javascript
// 在浏览器控制台执行
// 1. 检查 autoCalculate 状态
console.log('Auto calculate:', autoCalculate);

// 2. 手动触发排版计算
const result = await projectsApi.calculateLayout('project-id', {
  config: {
    tileWidth: 800,
    tileHeight: 800,
    gapWidth: 3,
    direction: 'horizontal',
    startPoint: { x: 0, y: 0 },
  },
});
console.log('Layout result:', result);

// 3. 检查 API 响应
api.get('/projects/').then(res => console.log(res));
```

### 后端调试

```python
# 在 layout_engine.py 中添加调试输出
import logging

logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)

def calculate_layout(self):
    logger.debug(f"Room polygon: {self._room_pts}")
    logger.debug(f"Tile size: {self.tile_width}x{self.tile_height}")
    # ... 其余代码
```

---

## 📝 测试检查清单

### 功能测试
- [ ] 新建项目并保存
- [ ] 绘制户型（3-8 个顶点）
- [ ] 选择不同瓷砖规格
- [ ] 更改铺贴方向
- [ ] 调整留缝宽度
- [ ] 保存后自动计算排版
- [ ] 自动跳转到预览页面
- [ ] 预览页面显示正确排版

### 边界测试
- [ ] 最小户型（3 个顶点）
- [ ] 复杂户型（8+ 个顶点）
- [ ] 极小瓷砖（100×100mm）
- [ ] 极大瓷砖（1200×2400mm）
- [ ] 零留缝（0mm）
- [ ] 大留缝（20mm）

### 错误处理
- [ ] 未绘制户型直接保存
- [ ] 瓷砖尺寸为 0
- [ ] 留缝宽度为负数
- [ ] 网络断开
- [ ] 后端服务未启动

### 用户体验
- [ ] 加载状态显示正确
- [ ] 错误消息清晰易懂
- [ ] 成功提示明确
- [ ] 页面跳转流畅
- [ ] 自动计算开关有效

---

## 🎯 验收标准

### 必须满足
1. ✅ 保存项目后 100% 自动触发排版计算
2. ✅ 计算成功后自动跳转到预览页面
3. ✅ 预览页面正确显示瓷砖填充效果
4. ✅ 统计信息准确（整砖/切割砖数量）

### 应该满足
1. ✅ 计算时间 < 3 秒（200㎡户型）
2. ✅ 错误处理友好，不会崩溃
3. ✅ 支持手动重新计算
4. ✅ 自动计算开关可控制

### 可以优化
1. ⚡ 添加计算进度条
2. ⚡ 支持后台异步计算
3. ⚡ 缓存计算结果
4. ⚡ 支持多种排版方案对比

---

## 📚 相关文档

- [排版引擎实现](file:///f:/目录已检查/TileLayout%20AI/backend/app/services/layout_engine.py)
- [ProjectEdit 组件](file:///f:/目录已检查/TileLayout%20AI/packages/shared/src/pages/ProjectEdit.tsx)
- [Projects API 服务](file:///f:/目录已检查/TileLayout%20AI/packages/shared/src/src/services/projects.ts)
- [后端 API 路由](file:///f:/目录已检查/TileLayout%20AI/backend/app/api/projects.py)

---

## 🚀 下一步

自动排版填充功能已修复，现在可以：

1. **测试完整流程** - 按照上述步骤验证功能
2. **优化用户体验** - 添加进度提示、动画效果
3. **性能优化** - 缓存、异步计算
4. **功能增强** - 多种排版方案对比、优化算法

---

**修复完成时间**: 2026-05-06  
**测试状态**: 待测试  
**验收状态**: 待验收
