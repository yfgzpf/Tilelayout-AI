# 排砖宝 AI 编辑器配置
# 将此文件用于 Cursor/Copilot/Cline 等 AI 编程工具

## 项目上下文
你正在开发「排砖宝 (TileLayout AI)」——一款瓷砖排版与销售闭环全链路工具。核心用户是瓷砖门店和设计师。

## 技术栈
- 前端: React 18 + TypeScript + Vite + Ant Design
- 后端: Python 3.11+ FastAPI + SQLAlchemy async + SQLite/PostgreSQL
- 几何引擎: 纯 Python (无外部依赖，Sutherland-Hodgman 裁剪 + 鞋带公式)
- PPT生成: python-pptx
- PDF生成: reportlab

## 开发要求
1. 所有代码必须有完整类型定义
2. 异步操作必须 try/catch 并管理 loading/error 状态
3. 价格和商家信息后端必须二次校验权限
4. 免费会员不能看到真实商家信息和价格
5. 辅料计算不可省略：瓷砖胶、美缝剂、水泥砂、十字卡缺一不可
6. 生成的代码可直接运行，不允许有任何占位符

## 命名约定
- 前端文件: kebab-case (project-edit.tsx)
- 后端文件: snake_case (ppt_generator.py)
- React 组件: PascalCase
- API 路由函数: snake_case
- 类型/接口: PascalCase，不使用 I 前缀

## 与普通 Web 应用的区别
- 这是工业级工具，不是玩具项目
- 排版精度要求 100%，不能有任何取整误差
- 确认单是交付物，格式必须规范
- 辅料计算必须有公式说明
- 三端统一 (Web/Mobile/Desktop)

## 文件组织
- backend/app/services/ - 核心业务逻辑
- backend/app/api/ - API 路由
- packages/web/src/pages/ - 页面组件
- packages/web/src/components/ - 可复用组件
- packages/web/src/services/ - API 调用

## 验收标准
参阅验收标准 v1.5，特别注意：
- 1.3 辅料计算（最易遗漏）
- 2.1 确认单内容完整性
- 4.x 免费/会员权限差异
- 后端必须二次校验，不可信任前端
