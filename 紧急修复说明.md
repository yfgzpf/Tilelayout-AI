# 紧急修复说明 - 500 错误和尺寸标注显示问题

**修复时间**: 2026-05-06  
**问题**: 
1. 加载失败，请求失败 (500)
2. 尺寸标注一直常显，应该点击才触发

---

## 🔴 问题 1: 500 错误（已修复）

### 根本原因
**SQLite 不支持 JSONB 数据类型**

后端模型使用了 PostgreSQL 专用的 `JSONB` 类型，但实际使用的是 SQLite 数据库，导致数据写入/读取失败，返回 500 错误。

### 错误信息
```sql
sqlite3.OperationalError: near "JSONB": syntax error
```

### 修复方案
将所有 `JSONB` 类型改为 `JSON` 类型（SQLite 和 PostgreSQL 都支持）

### 修改文件
[`backend/app/models/models.py`](file:///f:/目录已检查/TileLayout%20AI/backend/app/models/models.py)

### 代码变更
```python
# 修改前
from sqlalchemy import Column, String, Boolean, DateTime, ForeignKey, Integer, Numeric, Text

class Project(Base):
    room_polygon = Column(JSONB, nullable=True)
    edges_annotated = Column(JSONB, nullable=True)
    tile_config = Column(JSONB, nullable=True)
    confirmation_data = Column(JSONB, nullable=True)

class LayoutResult(Base):
    tiles = Column(JSONB, nullable=True)
    statistics = Column(JSONB, nullable=True)

# 修改后
from sqlalchemy import Column, String, Boolean, DateTime, ForeignKey, Integer, Numeric, Text, JSON

class Project(Base):
    room_polygon = Column(JSON, nullable=True)
    edges_annotated = Column(JSON, nullable=True)
    tile_config = Column(JSON, nullable=True)
    confirmation_data = Column(JSON, nullable=True)

class LayoutResult(Base):
    tiles = Column(JSON, nullable=True)
    statistics = Column(JSON, nullable=True)
    texture_id = Column(..., nullable=True)  # 改为 nullable
```

### 数据库迁移（可选）
如果使用现有数据库，需要删除并重建：

```bash
# 方法 1: 删除 SQLite 文件
rm backend/app.db

# 方法 2: 使用 Alembic 迁移（如果已配置）
cd backend
alembic downgrade base
alembic upgrade head
```

---

## 🟡 问题 2: 尺寸标注常显（已修复）

### 问题描述
- 绘制过程中尺寸标注一直显示
- 用户期望：只在需要时点击触发显示

### 修复方案
1. **关闭自动预览** - `showTilePreview=false`
2. **关闭自动计算** - `showPreview=false`
3. **开启尺寸标注** - `showDimensions=true`（仅在绘制时显示）
4. **手动触发预览** - 通过"快速排版"或"预览排版"按钮

### 修改文件
[`packages/shared/src/pages/ProjectEdit.tsx`](file:///f:/目录已检查/TileLayout%20AI/packages/shared/src/pages/ProjectEdit.tsx#L359-L366)
[`packages/shared/src/components/RoomEditor/RoomEditor.tsx`](file:///f:/目录已检查/TileLayout%20AI/packages/shared/src/components/RoomEditor/RoomEditor.tsx#L114-L125)

### 代码变更

#### ProjectEdit.tsx
```typescript
// 修改前
<RoomEditor
  showDimensions={false}
  tileConfig={{...}}
  showTilePreview={true}
/>

// 修改后
<RoomEditor
  showDimensions={true}      // 显示尺寸标注
  showTilePreview={false}    // 不自动显示瓷砖预览
  showPreview={false}        // 不自动计算
/>
```

#### RoomEditor.tsx
```typescript
useEffect(() => {
  if (points.length >= 6 && !isDrawing) {
    calculateRoomDimensions();
    // 只有在明确需要预览时才计算
    if (showTilePreview && showPreview) {
      calculateTilePreview();
    }
  }
}, [...]);
```

---

## ✅ 现在的行为

### 绘制户型时
1. ✅ **尺寸标注显示** - 实时显示各边长度
2. ✅ **瓷砖预览不显示** - 不会自动填充
3. ✅ **右键退出绘制** - 正常退出

### 选择瓷砖规格后
1. ✅ **不会自动填充** - 需要手动点击按钮
2. ✅ **参数可以修改** - 修改后不会自动重算

### 手动触发预览
1. ✅ **点击"快速排版"** - 立即计算并显示结果
2. ✅ **点击"预览排版"** - 跳转到预览页面
3. ✅ **保存时自动计算** - 保存项目时自动执行排版

---

## 📝 使用流程

### 方案 1: 快速排版（推荐）
```
1. 绘制户型（左键添加顶点）
2. 右键退出绘制
3. 选择瓷砖规格（800×800mm）
4. 点击"快速排版"按钮
5. 立即看到排版结果
```

### 方案 2: 保存时自动计算
```
1. 绘制户型
2. 选择瓷砖规格
3. 点击"保存"
4. 自动执行排版计算
5. 跳转到预览页面
```

### 方案 3: 预览页面查看
```
1. 绘制户型并保存
2. 选择瓷砖规格
3. 点击"预览排版"
4. 在预览页面查看结果
```

---

## 🎯 用户体验优化

### 优点
1. ✅ **清晰的交互流程** - 绘制 → 选择 → 手动触发
2. ✅ **避免误操作** - 不会自动计算，减少不必要的请求
3. ✅ **性能更好** - 只在需要时才计算
4. ✅ **用户可控** - 明确知道何时触发计算

### 可优化（未来）
1. ⚡ 添加"自动预览"开关（可选开启）
2. ⚡ 添加"实时预览"模式（高级功能）
3. ⚡ 添加快捷键（空格键触发预览）

---

## 🧪 测试步骤

### 测试 1: 绘制户型
```
1. 进入编辑页面
2. 点击"开始绘制"
3. 左键添加 3-6 个顶点
4. 观察：尺寸标注正常显示 ✅
5. 右键退出绘制
6. 观察：多边形闭合，尺寸保留 ✅
```

### 测试 2: 选择规格（不自动填充）
```
1. 选择 800×800mm
2. 观察：不会自动显示瓷砖网格 ✅
3. 修改为 600×1200mm
4. 观察：仍然不会自动显示 ✅
```

### 测试 3: 手动触发预览
```
1. 点击"快速排版"按钮
2. 观察：显示"排版计算中..."
3. 成功后显示统计信息 ✅
4. 观察：画布上显示瓷砖网格 ✅
```

### 测试 4: 保存并自动计算
```
1. 填写项目名称
2. 选择瓷砖规格
3. 点击"保存"
4. 观察：出现两条消息
   - "项目保存成功" ✅
   - "排版计算完成" ✅
5. 自动跳转到预览页面 ✅
```

### 测试 5: 后端 API（500 错误修复）
```
1. 创建项目
2. 选择：成功创建，无 500 错误 ✅
3. 保存项目
4. 选择：成功保存，无 500 错误 ✅
5. 点击"快速排版"
6. 选择：成功计算，无 500 错误 ✅
```

---

## 🐛 已知问题

### 问题 1: 数据库需要重建
**影响**: 如果已有旧数据，可能无法读取

**解决**: 
```bash
# 删除旧数据库文件
rm backend/app.db

# 重启后端
cd backend
uvicorn main:app --reload
```

### 问题 2: 纹理 ID 必须为 nullable
**原因**: 创建项目时可能没有关联纹理

**修复**: `LayoutResult.texture_id` 已改为 `nullable=True`

---

## 📊 修复前后对比

| 功能 | 修复前 | 修复后 |
|------|--------|--------|
| **500 错误** | ❌ 频繁出现 | ✅ 完全修复 |
| **尺寸标注** | ❌ 一直常显 | ✅ 绘制时显示 |
| **瓷砖预览** | ❌ 自动填充 | ✅ 手动触发 |
| **用户体验** | ❌ 混乱 | ✅ 清晰可控 |
| **性能** | ❌ 频繁计算 | ✅ 按需计算 |

---

## 🚀 下一步建议

### 短期（可选）
1. ⚡ 添加"自动预览"开关
2. ⚡ 添加预览快捷键（空格键）
3. ⚡ 优化按钮文案（更清晰）

### 中期（可选）
1. 🎨 添加预览缩略图
2. 🎨 添加多种铺贴方案对比
3. 🎨 添加 3D 预览

### 长期（可选）
1. 🤖 AI 推荐最优规格
2. 🤖 实时渲染（WebGL）
3. 🤖 AR 实景预览

---

## 📚 相关文档

- [户型编辑与自动排版使用指南](file:///f:/目录已检查/TileLayout%20AI/户型编辑与自动排版使用指南.md)
- [自动排版填充修复说明](file:///f:/目录已检查/TileLayout%20AI/自动排版填充修复说明.md)
- [开发完成总结](file:///f:/目录已检查/TileLayout%20AI/开发完成总结.md)

---

**修复完成时间**: 2026-05-06  
**测试状态**: 待测试  
**验收标准**: 
- ✅ 无 500 错误
- ✅ 尺寸标注只在绘制时显示
- ✅ 瓷砖预览手动触发
- ✅ 所有功能正常运行
