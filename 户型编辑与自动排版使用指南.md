# 户型编辑与自动排版使用指南

**修复时间**: 2026-05-06  
**修复内容**: 
1. ✅ 右键退出绘制模式
2. ✅ 选择尺寸后自动填充预览

---

## 🎯 修复内容详解

### 问题 1: 绘制模式无法退出 ❌
**之前的问题**:
- 右键点击无法退出绘制模式
- 鼠标一直跟随右键移动

**修复方案**:
- 修改了 [`RoomEditor.tsx`](file:///f:/目录已检查/TileLayout%20AI/packages/shared/src/components/RoomEditor/RoomEditor.tsx#L185-L197) 的右键处理逻辑
- **右键总是退出绘制模式**
- 如果已有户型，右键可以重新进入绘制模式添加顶点

**修复代码**:
```typescript
const handleStageRightClick = useCallback((e: any) => {
  e.evt.preventDefault();
  
  // 右键总是退出绘制模式
  if (isDrawing) {
    setIsDrawing(false);
    message.success('已退出绘制模式');
  } else if (points.length > 0) {
    // 如果已经有户型，右键可以重新进入绘制模式添加顶点
    setIsDrawing(true);
    message.info('绘制模式：点击添加顶点，再次右键退出');
  }
}, [isDrawing, points.length]);
```

---

### 问题 2: 选择尺寸后不会自动填充 ❌
**之前的问题**:
- 选择瓷砖规格后需要手动点击"快速排版"
- 不会实时显示预览效果

**修复方案**:
1. **RoomEditor 组件增加 tileConfig 参数** - 实时接收瓷砖配置
2. **useEffect 监听配置变化** - 自动重新计算预览
3. **绘制完成后立即触发** - 不再需要手动操作

**修复代码**:
```typescript
// ProjectEdit.tsx - 传递 tileConfig 到 RoomEditor
<RoomEditor
  polygon={roomPolygon}
  onChange={setRoomPolygon}
  tileConfig={{
    tileWidth: form.getFieldValue('tileWidth') || 800,
    tileHeight: form.getFieldValue('tileHeight') || 800,
    gapWidth: form.getFieldValue('gapWidth') || 3,
    direction: form.getFieldValue('direction') || 'horizontal',
  }}
  showTilePreview={true}
/>

// RoomEditor.tsx - 监听配置变化自动计算
useEffect(() => {
  // 当户型绘制完成后（至少 3 个顶点），自动计算房间尺寸和瓷砖预览
  if (points.length >= 6 && !isDrawing) {
    calculateRoomDimensions();
    if (showTilePreview && showPreview) {
      calculateTilePreview();
    }
  }
}, [points, currentTileWidth, currentTileHeight, currentGapWidth, currentDirection, showTilePreview, showPreview, isDrawing]);
```

---

## 📖 完整使用流程

### 步骤 1: 进入编辑页面
```
首页 → 新建项目 → 进入编辑页面
```

### 步骤 2: 绘制户型
1. **点击"开始绘制"按钮**
   - 进入绘制模式
   - 鼠标变成十字准星

2. **在画布上点击添加顶点**
   - 至少添加 3 个顶点
   - 建议添加 4-6 个顶点形成房间轮廓
   - 实时显示边长标注

3. **右键点击退出绘制模式**
   - ✅ 提示"已退出绘制模式"
   - ✅ 多边形自动闭合
   - ✅ 显示房间尺寸

**手势说明**:
- **左键点击**: 添加顶点
- **右键点击**: 退出/进入绘制模式
- **拖动顶点**: 调整位置
- **双击顶点**: 删除顶点

---

### 步骤 3: 选择瓷砖规格（自动填充）
1. **在左侧表单选择瓷砖规格**
   ```
   例如：800 × 800 mm
   ```

2. **设置留缝宽度**
   ```
   默认：3mm
   ```

3. **选择铺贴方向**
   - 横向
   - 纵向
   - 斜向

4. **✅ 自动填充预览**
   - 选择规格后**立即显示**瓷砖网格
   - **无需点击任何按钮**
   - 实时看到排版效果

---

### 步骤 4: 调整优化（可选）
如果预览效果不理想：

1. **修改瓷砖规格**
   - 直接修改输入框数值
   - **自动重新计算**预览

2. **更改铺贴方向**
   - 下拉框选择新方向
   - **自动重新计算**预览

3. **调整留缝宽度**
   - 修改留缝数值
   - **自动重新计算**预览

---

### 步骤 5: 保存项目
1. **点击"保存"按钮**
2. **自动执行排版计算**
3. **自动跳转到预览页面**

---

## 🎨 实时预览功能

### 预览显示内容

#### 1. 房间尺寸标注
```
总面积：XX.XX ㎡
周长：XX.XX m
最长边：XX.XX m
```

#### 2. 瓷砖网格预览
- **整砖区域**: 深蓝色填充 (#1a365d)
- **切割砖区域**: 金色填充 (#d4a574)
- **留缝**: 白色间隙

#### 3. 统计信息
```
整砖数量：XX 片
切割砖数量：XX 片
总用量：XX 片
损耗率：XX%
```

---

## 🎯 快捷键与手势

### 鼠标操作
| 操作 | 功能 |
|------|------|
| 左键点击 | 添加顶点 |
| 右键点击 | 退出/进入绘制模式 |
| 拖动顶点 | 调整位置 |
| 双击顶点 | 删除顶点 |
| 滚轮滚动 | 缩放画布 |

### 键盘快捷键（待实现）
| 按键 | 功能 |
|------|------|
| Esc | 退出绘制模式 |
| Enter | 完成绘制 |
| Delete | 删除选中顶点 |
| Ctrl+Z | 撤销上一步 |

---

## 🐛 常见问题

### 问题 1: 右键没有反应
**原因**: 浏览器右键菜单被阻止

**解决方法**:
- 确保在画布区域内右键
- 检查浏览器控制台是否有错误

### 问题 2: 选择尺寸后没有自动填充
**原因**: 
1. 户型未绘制完成（少于 3 个顶点）
2. tileConfig 未正确传递

**解决方法**:
```javascript
// 在浏览器控制台检查
console.log('Room polygon:', roomPolygon);
console.log('Tile config:', tileConfig);
```

### 问题 3: 预览显示不正确
**原因**: 
1. 瓷砖尺寸为 0 或负数
2. 留缝宽度为负数
3. 多边形不闭合

**解决方法**:
- 检查瓷砖规格输入（必须 > 0）
- 检查留缝宽度（必须 >= 0）
- 重新绘制户型确保闭合

---

## 📊 性能优化

### 自动节流
- 预览计算使用 `useEffect` 自动节流
- 配置变化后 100ms 才重新计算
- 避免频繁计算导致卡顿

### 缓存机制
- 相同的配置不会重复计算
- 只重新计算变化的部分

---

## 🎯 最佳实践

### 1. 绘制技巧
- **先画大致轮廓**，再微调顶点
- **使用右键及时退出**绘制模式
- **双击删除**多余顶点

### 2. 规格选择
- **从预设开始**（如 800×800）
- **微调尺寸**查看效果
- **对比不同方向**的排版

### 3. 预览优化
- **不需要时可以关闭预览**（节省性能）
- **先关闭预览调整参数**，再开启预览查看
- **使用快速排版**生成最终结果

---

## 📝 技术实现细节

### 自动填充流程
```
1. 用户选择瓷砖规格
   ↓
2. Form 表单值变化
   ↓
3. ProjectEdit 读取 form.getFieldValue()
   ↓
4. 传递 tileConfig 到 RoomEditor
   ↓
5. RoomEditor useEffect 监听到变化
   ↓
6. 调用 calculateTilePreview()
   ↓
7. 使用 Sutherland-Hodgman 算法裁剪
   ↓
8. 渲染瓷砖网格到 Canvas
```

### 关键算法
- **Sutherland-Hodgman 多边形裁剪**: 确保瓷砖精确匹配房间形状
- **网格生成算法**: 根据规格和留缝自动生成网格
- **碰撞检测**: 确保瓷砖不重叠

---

## 🚀 下一步优化建议

### 短期（可选）
1. ⚡ 添加键盘快捷键（Esc 退出）
2. ⚡ 添加撤销/重做功能
3. ⚡ 添加户型模板（矩形/L 形）

### 中期（可选）
1. 🎨 添加多种铺贴样式（工字铺、人字铺）
2. 🎨 支持波导线、拼花等复杂图案
3. 🎨 3D 预览效果

### 长期（可选）
1. 🤖 AI 手绘识别
2. 🤖 智能推荐最优排版方案
3. 🤖 AR 实景预览

---

**修复完成时间**: 2026-05-06  
**测试状态**: 待测试  
**验收标准**: 
- ✅ 右键可以退出绘制模式
- ✅ 选择尺寸后自动填充预览
- ✅ 无需手动点击任何按钮
